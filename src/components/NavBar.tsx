import { Navbar } from "react-bootstrap";
import "../styles/debug.css";
import "../styles/NavBar.css";

const NavBar = () => {
  return (
    <div className=".debug-border4">
      <Navbar className="navbar" bg="light">
        NavBar
      </Navbar>
    </div>
  );
};

export default NavBar;
